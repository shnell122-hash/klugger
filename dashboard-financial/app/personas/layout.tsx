import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Comprar propiedad · Klugger",
  description: "Encuentra y compra tu próxima propiedad con datos reales: verificación itemizada, plusvalía transparente y una shortlist para decidir en familia.",
};

export default function PersonasLayout({ children }: { children: React.ReactNode }) {
  return children;
}
