# URL interna del backend Express (server-side, rewrite de Next.js)
# En producción apunta a localhost porque Next.js y el backend están en el mismo servidor
BACKEND_INTERNAL_URL=http://localhost:3010

# Para desarrollo local:
# BACKEND_INTERNAL_URL=http://localhost:3010
